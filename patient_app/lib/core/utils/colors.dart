import 'package:flutter/material.dart';

class AppColors {
  // Primary teal/green
  static const Color primary = Color(0xFF2EAA8A);       // Teal green
  static const Color primaryLight = Color(0xFF3DBFA0);  // Lighter teal
  static const Color primaryDark = Color(0xFF1E8A6E);   // Dark teal

  // Accent blue (little touch)
  static const Color accent = Color(0xFF2563EB);        // Blue accent

  // Backgrounds (light/white theme)
  static const Color background = Color(0xFFF0FAF7);    // Very light mint
  static const Color surface = Color(0xFFFFFFFF);       // White surface
  static const Color cardBackground = Color(0xFFFFFFFF);// White card
  static const Color inputBackground = Color(0xFFF5FBF9); // Light mint input
  static const Color white = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF1B3A4B);   // Dark blue-gray
  static const Color textSecondary = Color(0xFF7A9BAE); // Muted blue-gray
  static const Color textHint = Color(0xFFAABDC8);      // Hint text

  // Status
  static const Color available = Color(0xFF2EAA8A);
  static const Color availableLight = Color(0xFFE8F5F1);
  static const Color star = Color(0xFFFFC107);           // Amber

  // Border
  static const Color border = Color(0xFFDDE8E5);        // Subtle green border
  static const Color borderFocused = Color(0xFF2EAA8A); // Green focus border
}
